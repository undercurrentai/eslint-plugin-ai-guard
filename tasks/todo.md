# tasks/todo.md

> Boris Cherny self-improvement loop. Append plan items at session start, check off as you complete, prune at session end.
> Pair with `tasks/lessons.md` (rules learned from corrections).

## Active

- [ ] Observe new CLAUDE.md v4.0 and `.claude/settings.json` for 30 days (until 2026-05-17). Capture friction in `lessons.md`. Tune Ask-First triggers if any fire >5×/week without value.
- [ ] Re-audit at `2026-10-17` per CLAUDE.md §12 expiry.

## Watch (deferred — re-check at expiry)

- [ ] ESLint 10 ecosystem maturity. When community plugins broadly support 10, widen `peerDependencies.eslint` to `^9.0.0 || ^10.0.0` and bump `engines.node` to `>=20.19.0`. Coordinate with v3.0.0 major.
- [ ] typescript-eslint issue [#11543](https://github.com/typescript-eslint/typescript-eslint/issues/11543) — `RuleCreator` type drift vs downstream `defineConfig()`. Track upstream resolution; ship guidance update if a workaround becomes recommended.
- [ ] Dual release workflow (`publish.yml` OIDC + `release.yml` `NPM_TOKEN`). Decide which is canonical; archive the other to avoid double-publish risk on `v*` tag + GitHub Release sequence. See audit `Lock-Step Dependencies`.
- [ ] **Dual-track upstream promise — enact before 2026-05-02.** Audit 2026-04-18 (`docs/claude/audits/upstream-dual-track-2026-04-18.md`) found zero courtesy PRs to `YashJadhav21/eslint-plugin-ai-guard` despite written policy in `CONTRIBUTING.md:8`, `README.md:222`, `CHANGELOG.md:179`. Upstream is active (pushed yesterday, 9 stars). Five cycle-1 fixes fit upstream scope — 3 security (`no-hardcoded-secret` quoted keys, `no-eval-dynamic` bare `Function()`, `no-sql-string-concat` mixed template+concat), 2 async (`no-floating-promise` nested-function stop, `no-async-array-callback` `ExportNamedDeclaration` unwrap). Move A: ship security-3 as one courtesy PR, async-2 as another. Move B if no response in 2 weeks: open a tracking issue. Move C (email via `jadhavyash.in`) only if A+B sit idle for a month. If Move A is deferred past 2026-05-31, soften the dual-track wording in README/CONTRIBUTING to intent-language (Option 2 in the audit) to avoid asserting an untrue past-tense claim.
- [ ] **CI workflow registration — partially done 2026-04-20**. Committed a substantive header-comment edit to `.github/workflows/ci.yml` and `stale.yml` to trigger GitHub Actions' workflow-file rescan. After the next push on main, both should register. `publish.yml` and `release.yml` remain DELIBERATELY unregistered — each has a missing prerequisite: `release.yml` needs an `NPM_TOKEN` repo secret (empty via `gh secret list`), `publish.yml` needs the `npm-publish` environment (empty via `gh api /environments`) plus npm OIDC trusted publisher configuration on the npm-registry side. Those prerequisites are owner-interactive and gated on the publish-method decision (see CHANGELOG `[Unreleased]` + the open questions in `~/.claude/plans/workflow-registration-fix.md`). Verify after merge: `gh workflow list` should show `CI` and `Mark stale issues` as active.

## Done

- [x] 2026-04-18 — Add CI mirror-drift guard (`tests/configs/mirror.test.ts`, 5 assertions). Closes audit Self-Red-Team #1 (R=12, highest residual risk): drift between `src/configs/{recommended,strict,security}.ts` and `cli/utils/eslint-runner.ts:77-114` now fails the test suite. Catches severity flips, membership mismatches, unregistered rule IDs, and stray `off` entries in CLI maps. 627 → 632 tests.
- [x] 2026-04-18 — Quality-gate Phase 2 bug-hunt cycle 1: 14 rule + CLI correctness fixes via hybrid Codex + Claude sweep (3 Codex iterations, 3 parallel Claude partitions). Closed 3 security FN classes (hardcoded-secret bracket forms, eval-dynamic bare Function() RCE, sql-concat mixed templates), 1 async-scope FN (nested try/catch suppression), 1 async-array FP (exported map pattern), 3 CLI correctness bugs (baseline --mode return, baseline --check preset, loadBaseline schema), SIGINT exit code, defineConfig flat-config patching, favicon public-route regex over-match, webhook chained-route form, STOP_DESCENT class methods. 632 → 645 tests; npm audit 0 vulnerabilities. 7 LOW CLI UX findings logged to `.quality-gate/cycle-1-deferred-findings.md` for a focused follow-up PR.
- [x] 2026-04-18 — Fixed pre-existing dead link in `docs/migration/v1-to-v2.md:169` (`../../CHANGELOG.md` → absolute GitHub URL). `npm run docs:build` was failing silently; now green.
- [x] 2026-04-18 — Added CHANGELOG `[Unreleased]` section covering the quality-gate cycle. Per CLAUDE.md §5.1 step 8 (mandatory for any rule/CLI change).
- [x] 2026-04-18 — **Dual-track upstream promise: first courtesy PR shipped.** [`YashJadhav21/eslint-plugin-ai-guard#2`](https://github.com/YashJadhav21/eslint-plugin-ai-guard/pull/2) — `fix(no-eval-dynamic): flag bare Function(...) (without new)`. First-ever PR against upstream (prior `gh pr list --state all` returned `[]`). CHANGELOG `[Unreleased]` now carries an "Upstream cross-PRs" section — the first entry. Policy is now honest, not just documented. Audit `docs/claude/audits/upstream-dual-track-2026-04-18.md` updated-by-reference. Remaining 4 upstream-scope candidates (no-hardcoded-secret, no-sql-string-concat, no-floating-promise, no-async-array-callback) held pending Yash's response to PR #2 — if accepted/engaged, batch as 1–2 follow-up PRs; if silent for 2 weeks, escalate to Move B (tracking issue).
- [x] 2026-04-18 — **Strategic plan**: `~/.claude/plans/this-is-a-strange-eager-hennessy.md` — evidence-based assessment ("is the plugin solving a real problem?"). Verdict: problem is real; ~65% of rules overlap with `@typescript-eslint`/`eslint-plugin-security`/Semgrep; ~35% irreducible novel (the framework-aware trio + a few smaller rules). Approved repositioning path; rule deprecations deferred (cycle-1 made `no-eval-dynamic` and `no-hardcoded-secret` genuinely novel vs `eslint-plugin-security`, so they stay).
- [x] 2026-04-18 — **Narrative repositioning shipped** (commit `f5bd52e`). Plugin now leads with "framework-aware security lint for JS/TS routes and webhooks" in `package.json` description, `README.md` tagline + lede, `docs/index.md`, and `CHANGELOG.md [Unreleased]`. AI-code framing moved to a secondary "Why AI-generated code is a common trigger" subsection.
- [x] 2026-04-19 — **Docs-sync follow-through merged as PR #6** (squash commit `b2fd5f5`). Propagated repositioning to `docs/migration/v1-to-v2.md`, `docs/guides/ci-integration.md`, `docs/guides/migrating-existing-project.md`, `docs/cli/overview.md`, `docs/cli/run.md`, `CONTRIBUTING.md`; added README pre-release banner; taxonomy fix in `docs/rules.md`; mirror-test + `L004` references added to CONTRIBUTING step 4.
- [x] 2026-04-20 — **Stale branch cleanup**: pruned 4 merged remote branches (`feat/m1-compat-deprecations`, `feat/m2-framework-auth-trio`, `bug-hunt/m3-async-array-callback-identifier`, `refactor/shared-async-scope-helpers`) + their local counterparts. Only `main` remains. PR #2 upstream still OPEN / 0 comments; Yash last active on `2026-04-17`. Staying inside the 2-week Move-A window (expires 2026-05-02) before escalating.
- [x] 2026-04-20 — **`CLAUDE.md §1 purpose statement aligned** with the new framework-aware security positioning. Also corrected the misleading "a published npm library" phrasing — the package is `v2.0.0-beta.2` local only; publish still pending CI workflow registration fix.
